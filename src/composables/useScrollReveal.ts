/**
 * Scroll Reveal Composable
 * Now using native CSS animations via IntersectionObserver
 * Zero dependencies - removed GSAP for ~43KB bundle savings
 */
export { useScrollReveal } from './useScrollRevealNative'
